# Design QA — 真实本地比较 UI

## 验证基准

- 实现地址：`http://localhost:4173`
- 桌面视口：1440 × 1024 CSS px。
- 移动视口：390 × 844 CSS px。
- QA 输入：脚本运行时在系统临时目录生成的匿名 DOCX；运行结束即删除。
- QA 截图：仅生成在被 Git 忽略的 `ui/qa-artifacts/`，发布前删除。

## 必须验证

- 首屏为空白出厂状态，没有示例简历、岗位或候选结果。
- 用户提供 DOCX 与岗位文字后才允许真实运行。
- 比较页原文逐字来自冻结基线；候选逐字来自真实 Skill ChangeSet。
- 每个候选都能显示 Skill id、版本、invocation id 与运行状态。
- Provider 失败、超时、拒绝或无法映射原文时，不生成替代候选。
- 原稿区约占工作区一半，并按真实 `sourceBlockRef` 定位当前段落。
- 封面与工作台沿用已确认的纸张、黑墨和红黄蓝套印视觉语言。
- 桌面与移动首屏无文字裁切、横向溢出或控制台错误。

## 发布约束

- 仓库不得包含个人简历、岗位、运行结果、缓存、个人路径或真实运行截图。
- `npm run verify:release` 必须通过后才允许发布。
- 当前真实范围为 DOCX、岗位纯文本与本机已安装 Skill。PDF、图片 OCR、在线 Skill 安装和最终文件导出尚未接通，不得在 UI 或 README 中宣称已完成。

## 本轮结果

- 真实 Provider：`resume-optimizer`
- 通过来源与事实门禁的候选区块：4
- 原稿区工作区占比：0.5
- 关键界面状态：7
- 浏览器控制台错误：0
- 验收输入与截图均为本机临时产物，不进入发布包。

最终结果：passed
